# → /etc/systemd/system/user.slice.d/50-memoryhigh.conf
# 所有用户会话（tmux 里的 claude）总内存超此软顶 → 内核提前回收/换页，留余量给系统/ssh，
# 别等到全局 OOM。软顶（MemoryHigh）只节流+换页、不硬杀。__MEMHIGH__ 由 oom/harden.sh 按 RAM×0.8 填。
[Slice]
MemoryHigh=__MEMHIGH__
