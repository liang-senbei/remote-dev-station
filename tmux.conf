set -g mouse on
# 让 tmux 通过 OSC52 把复制内容送到「本地(Mac)系统剪贴板」
set -g set-clipboard on
set -as terminal-features ',*:clipboard'
# 多客户端 attach 同一会话时，按「最近活动的客户端」定尺寸，而非缩到最小屏
set -g window-size latest
setw -g aggressive-resize on
